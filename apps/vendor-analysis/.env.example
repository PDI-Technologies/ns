# NetSuite Account ID
NS_ACCOUNT_ID=your_account_id

# ============================================================================
# NetSuite Authentication Credentials
# Choose ONE method based on config.yaml auth_method setting
# ============================================================================

# OPTION 1: TBA (Token-Based Authentication) - OAuth 1.0
# DEPRECATED: Will be removed February 2025
# Requires 4 credentials: Consumer Key/Secret + Token ID/Secret
#
# From Integration Record (Setup > Integration > Manage Integrations):
NS_CONSUMER_KEY=your_consumer_key_from_integration_record
NS_CONSUMER_SECRET=your_consumer_secret_from_integration_record
#
# From Access Token (Setup > Users/Roles > Access Tokens):
NS_TOKEN_ID=your_token_id_from_access_token
NS_TOKEN_SECRET=your_token_secret_from_access_token

# OPTION 2: OAuth 2.0 Client Credentials (Recommended 2025+)
# Requires 2 credentials: Consumer Key/Secret only
# Note: Currently requires certificate-based authentication setup
#
# From Integration Record (Setup > Integration > Manage Integrations):
# NS_CONSUMER_KEY=your_oauth2_consumer_key
# NS_CONSUMER_SECRET=your_oauth2_consumer_secret

# ============================================================================
# Database Configuration
# Database host/port/name configured in config.yaml
# Only credentials are in .env
# ============================================================================

# For Supabase local instance (default):
DB_USER=supabase_admin
DB_PASSWORD=postgres

# For standalone PostgreSQL:
# DB_USER=postgres
# DB_PASSWORD=your_password
