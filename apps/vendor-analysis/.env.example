# NetSuite OAuth 2.0 (Required as of Feb 2025)
# Get these from NetSuite: Setup > Integration > Manage Integrations
NS_ACCOUNT_ID=your_account_id
NS_CLIENT_ID=your_consumer_key
NS_CLIENT_SECRET=your_consumer_secret

# Database Configuration
# Database host/port/name configured in config.yaml
# Only credentials are in .env

# For Supabase local instance (default):
DB_USER=supabase_admin
DB_PASSWORD=postgres

# For standalone PostgreSQL:
# DB_USER=postgres
# DB_PASSWORD=your_password
